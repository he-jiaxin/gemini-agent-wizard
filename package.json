{
  "name": "create-gemini-agent",
  "version": "1.9.2",
  "type": "module",
  "bin": {
    "create-gemini-agent": "./dist/index.js",
    "gemini-subagent": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx index.ts",
    "build": "tsup",
    "prepublishOnly": "npm run build",
    "version": "node -e \"const pkg=require('./package.json'); const ext=require('./gemini-extension.json'); ext.version=pkg.version; require('fs').writeFileSync('gemini-extension.json', JSON.stringify(ext, null, 2) + '\\n');\""
  },
  "description": "An interactive subagent creator for the Gemini CLI",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "picocolors": "^1.1.1"
  }
}
